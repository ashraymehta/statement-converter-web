# compiled output
/dist
/tmp

# Playwright
/test-results
/playwright-report
/blob-report

# dependencies
/node_modules
# Vite dev cache (created when root: 'src' is set)
src/node_modules/.vite

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history/*

# misc
/coverage
npm-debug.log
yarn-error.log

# System Files
.DS_Store
Thumbs.db

# Real statement files — add to a local `real-statements/` directory (git-ignored) for manual testing.
# The files under src/test/fixtures/ are synthetic/fabricated and are committed intentionally.
real-statements/
